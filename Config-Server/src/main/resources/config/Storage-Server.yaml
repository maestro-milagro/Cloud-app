server:
  port: 8087

eureka:
  client:
    serviceUrl:
      defaultZone: http://service-registry:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

spring:
  data:
    mongodb:
      uri: mongodb://mongo:27017/db